// import { render } from '@testing-library/react';
// import { Chip } from './chip';

// test("Should display a basic Chip- with default param", () => {
//     const { container } = render(<Chip text="basic Chip" />);
//     const linkElement = container.querySelector('a');

//     expect(linkElement).not.toBeNull();
//     // expect(linkElement).toHaveAttribute('href', 'https://pagily.io/'); // .toHaveAttribute rqequire jest-dom : https://stackoverflow.com/a/65948977
//     expect(linkElement?.textContent).toEqual('Let\'s go !');
// });